const expect = require('chai').expect;
const sinon = require('sinon');

const Review = require('../../server/resources/Product/Review.js');

// it should test that retrieveOne returns the correct data. 


// it should test that retrieve returns an array of the correct length.